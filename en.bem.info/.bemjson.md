## 💎 Định Nghĩa BEMJSON Tối Giản (Cho Input React Component)

BEMJSON được sử dụng ở đây như một ngôn ngữ cấu hình để mô tả cấu trúc lồng nhau (nesting) và các biến thể (variants) của các Component React.

### I. Các Trường Định Danh Cấu Trúc BEM (Bắt buộc)

Các trường này xác định thực thể BEM đang được mô tả và vị trí của nó trong cây cấu trúc:

#### 1\. `block`

- **Kiểu dữ liệu:** `{String}`
- **Mô tả:** Tên của Khối (Block). Trong React, đây là tên của Component chính.
- **Ví dụ:**

  JSON

      { "block": "Card" }

#### 2\. `elem`

- **Kiểu dữ liệu:** `{String}`
- **Mô tả:** Tên của Phần tử (Element). Nó mô tả một phần của Khối cha (nếu `block` của node đó bị bỏ qua và được kế thừa từ ngữ cảnh).
- **Ví dụ:**

  JSON

      { "block": "Card", "content": { "elem": "title" } }

#### 3\. `mods`

- **Kiểu dữ liệu:** `{Object}`
- **Mô tả:** Hash các Block Modifier. Dùng để truyền các biến thể hoặc trạng thái của Khối dưới dạng props.
- **Giá trị:** Cặp khóa-giá trị (ví dụ: `"size": "large"`, `"disabled": true`).
- **Ví dụ:**

  JSON

      { "block": "Button", "mods": { "theme": "primary", "disabled": true } }

### II. Các Trường Cấu Trúc và Nội dung

#### 5\. `content`

- **Kiểu dữ liệu:** `{*}` (Có thể là `String`, `Array`, `Object`, hoặc `null`)
- **Mô tả:** Nội dung con. Trường này chứa các node con khác của thực thể BEM hiện tại.

  - **String:** Được xem là nội dung văn bản.
  - **Array:** Chứa một danh sách các node BEMJSON con.
  - **Object:** Là một node BEMJSON lồng nhau (Block hoặc Element khác).

- **Ví dụ:**

  JSON

      {
        "block": "Menu",
        "content": [
          { "elem": "item", "content": "Mục 1" },
        ]
      }
